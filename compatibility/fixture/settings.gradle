pluginManagement {
    repositories {
        maven { url uri('../../build/compat-maven-cache') }
        google()
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
}

rootProject.name = 'x2c-agp-compatibility-fixture'
include ':business-base'
