package dev.x2c.plugin.api;

/** Android Activity launch modes supported by the fixed host container pool. */
public enum PluginLaunchMode {
    STANDARD,
    SINGLE_TOP,
    SINGLE_TASK,
    SINGLE_INSTANCE
}
