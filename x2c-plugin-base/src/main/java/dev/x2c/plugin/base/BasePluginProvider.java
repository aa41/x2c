package dev.x2c.plugin.base;

import dev.x2c.plugin.runtime.PluginContentProvider;

/** Stable host-owned root for ContentProvider base classes shared by plugin payloads. */
public abstract class BasePluginProvider extends PluginContentProvider {
    protected BasePluginProvider() {}
}
